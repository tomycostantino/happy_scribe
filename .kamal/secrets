# Secrets fetched from 1Password via the Kamal secrets adapter.
# Vault: Development, Item: HappyScribe
#
# Required 1Password fields:
#   KAMAL_REGISTRY_PASSWORD - Docker Hub access token
#   RAILS_MASTER_KEY        - Rails master key (from config/master.key)

SECRETS=$(kamal secrets fetch --adapter 1password --account my.1password.com --from Development/HappyScribe KAMAL_REGISTRY_PASSWORD RAILS_MASTER_KEY)

KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD ${SECRETS})
RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})
