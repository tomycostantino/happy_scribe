<% content_for :title, "New chat" %>

<div class="w-full max-w-lg mx-auto p-8">
  <div class="mb-6">
    <%= back_link "Back to chats", chats_path %>
  </div>

  <h1 class="text-xl font-bold text-gray-900 mb-6">New chat</h1>

  <%= render "form", chat: @chat %>
</div>
